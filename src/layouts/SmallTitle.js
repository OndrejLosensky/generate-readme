import React from "react";

const SmallTitle = (prompts) => {
    return(
        <div>
            <h2 className="text-lg uppercase text-whiteText"> {prompts.text} </h2>
        </div>
    )
}
export default SmallTitle